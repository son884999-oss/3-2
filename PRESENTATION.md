# Balance AI 발표·질의응답 가이드

## 1. 30초 소개

“Balance AI는 날짜별 수면시간과 운동시간을 함께 기록하고 분석하는 건강관리 에이전트입니다. 일반적인 챗봇은 사용자의 실제 기록을 모르지만, 이 서비스는 Firestore에 저장된 데이터를 매 질문마다 요약해 Gemini의 시스템 지시문에 넣습니다. 그래서 평균 수면, 운동량, 최근 추세 같은 실제 수치를 근거로 답합니다. 데이터 CRUD, 대화 저장과 불러오기, Render와 Vercel 배포까지 하나의 웹 서비스로 완성했습니다.”

## 2. 3분 발표 대본

### 문제와 주제 선정

일반 챗봇은 사용자의 생활 기록을 모르기 때문에 답변이 일반론에 머뭅니다. 저는 서로 영향을 주는 수면시간과 운동시간을 하나의 건강 시계열 데이터로 묶고, 내 기록을 아는 AI 코치를 만들었습니다. 실제 개인정보 없이 시연할 수 있도록 고정 난수 시드로 최근 120일의 가상 데이터를 생성했습니다.

### 데이터 처리

한 기록은 `date`, `value`, `memo`로 구성되고, `value` 안에 `sleep_hours`와 `exercise_minutes`가 들어갑니다. 데이터는 Firestore의 `data` 컬렉션에 저장합니다. 요약 API는 전체 기간과 개수, 수면·운동의 평균·최대·최소, 운동 합계를 계산합니다. 최근 7일 평균을 직전 7일과 비교해 증가·감소·유지도 판정합니다. 추가 지표로 수면 7~9시간과 운동 30분 이상을 동시에 충족한 건강 균형 달성률을 계산합니다.

### AI 컨텍스트 주입

채팅 요청이 들어오면 백엔드는 먼저 Firestore의 최신 데이터를 읽어 요약합니다. 이 요약을 시스템 지시문에 넣고 사용자 질문과 최근 대화 내용을 Gemini에 전달합니다. AI 답변과 사용자 질문은 `conversations` 컬렉션에 자동 저장됩니다. 따라서 사용자가 나중에 데이터를 추가하거나 수정하면 다음 질문부터 새 요약이 사용되고, 서버를 다시 시작하거나 AI를 다시 학습시킬 필요가 없습니다.

### 구조와 검증

FastAPI 코드는 역할별로 나눴습니다. `routers`는 HTTP 요청과 상태 코드를, `services`는 통계와 AI 호출을, `repository`는 Firestore 접근을, `models`는 Pydantic 검증을 담당합니다. 수면 0~24시간, 운동 0~1,440분, 미래 날짜와 빈 메시지 같은 잘못된 입력은 저장 전에 차단합니다.

프론트엔드는 과제 조건에 맞춰 HTML, CSS, JavaScript만 사용했습니다. 채팅 로딩 표시, 데이터 추가·수정·삭제, 대화 목록과 불러오기, Canvas 추세 그래프, CSV 다운로드와 다크 모드를 제공합니다. 백엔드는 Render, 프론트엔드는 Vercel에 배포했고 환경변수와 CORS로 두 서비스를 안전하게 연결했습니다.

### 마무리

최종적으로 저장된 데이터를 분석하고, 그 요약을 AI 컨텍스트로 사용하고, 대화 결과까지 다시 저장하는 전체 흐름을 구현했습니다. 현재 공개 서비스와 Swagger에서 직접 확인할 수 있습니다.

## 3. 시연 순서

1. 대시보드에서 데이터 개수, 평균 수면, 평균 운동, 달성률과 차트를 보여줍니다.
2. 채팅에 “최근 수면과 운동 추세를 분석해줘”라고 질문하고 로딩 표시와 데이터 기반 답변을 보여줍니다.
3. 건강 기록 화면에서 새 날짜의 데이터를 추가한 뒤 목록이 갱신되는 것을 보여줍니다.
4. 같은 기록을 수정하고 삭제해 CRUD를 확인합니다.
5. 대화 기록 화면에서 방금 저장된 대화를 선택해 메시지가 복원되는 것을 보여줍니다.
6. Swagger에서 `/api/data/summary`와 필수 API 목록을 보여줍니다.

Render 무료 서버는 한동안 사용하지 않으면 첫 요청에 약 1분이 걸릴 수 있습니다. 발표 직전에 프론트엔드와 Swagger를 한 번 열어 서버를 깨워 두는 것이 좋습니다.

## 4. 전체 동작 흐름

```text
사용자 입력
   ↓
Vercel의 바닐라 JavaScript 화면
   ↓ REST API
Render의 FastAPI Router
   ├─ Pydantic 입력 검증
   ├─ Repository → Firestore data 조회
   ├─ Service → 통계·최근 추세 요약
   ├─ 요약을 시스템 지시문에 주입 → Gemini 호출
   └─ 질문과 답변 → Firestore conversations 저장
   ↓
답변 표시 및 대화 불러오기
```

## 5. 예상 질문과 답변

### 왜 수면과 운동을 함께 선택했나요?

둘 다 날짜별로 누적되는 시계열 데이터이고 생활 습관의 균형을 함께 설명할 수 있기 때문입니다. 두 지표를 동시에 만족한 날짜 비율도 추가 지표로 만들 수 있어 단순 평균보다 유용합니다.

### 시계열 분석은 어떻게 하나요?

Firestore 기록을 날짜순으로 정렬한 뒤 전체 평균·최대·최소를 계산합니다. 최근 구간은 최근 7일과 직전 7일 평균의 차이를 비교합니다. 수면은 0.15시간, 운동은 3분을 넘게 변했을 때 증가나 감소로 보고 그 이하는 유지로 처리합니다.

### 컨텍스트 주입이 무엇인가요?

AI 모델을 다시 학습시키는 것이 아니라, 요청 시점의 데이터 요약을 시스템 지시문에 함께 넣는 방식입니다. 모델은 그 요청 안에서 기간, 개수, 통계와 추세를 참고해 답합니다.

### 데이터를 나중에 추가해도 반영되나요?

네. `/api/chat`은 질문할 때마다 Firestore에서 현재 데이터를 다시 조회하고 요약합니다. 추가·수정·삭제 결과가 다음 답변에 자동 반영됩니다.

### 왜 라우터, 서비스, 저장소를 분리했나요?

HTTP 처리, 비즈니스 로직, 데이터베이스 접근의 책임을 분리하기 위해서입니다. 통계 계산만 테스트하거나 Firestore·AI 제공자를 교체할 때 다른 부분의 변경을 줄일 수 있습니다.

### Pydantic은 왜 사용했나요?

프론트엔드 검증은 우회할 수 있으므로 서버에서도 데이터 형식과 범위를 보장해야 합니다. 날짜, 숫자 범위, 문자열 길이를 모델에 선언해 잘못된 데이터가 Firestore에 들어가기 전에 일관된 422 응답으로 차단합니다.

### Firestore 컬렉션은 어떻게 설계했나요?

`data`에는 날짜, 수면·운동 값, 메모를 저장합니다. `conversations`에는 제목, 전체 메시지 배열, 생성·수정 시각을 저장합니다. 문서 ID는 Firestore 자동 ID를 사용하고 API의 수정·삭제·단건 조회에 사용합니다.

### CORS와 환경변수가 왜 필요한가요?

프론트와 백엔드의 도메인이 서로 달라 브라우저가 허용된 출처인지 확인하므로 CORS 설정이 필요합니다. Gemini 키와 Firebase 서비스 계정은 공개 코드나 브라우저에 넣으면 탈취될 수 있어 Render의 환경변수와 Secret File로 관리합니다. Vercel에는 공개 가능한 API 주소만 둡니다.

### 왜 OpenAI가 아니라 Gemini인가요?

이번 구현에서는 발급받을 수 있었던 Google Gemini API 키를 사용했습니다. 데이터 요약을 시스템 지시문에 주입하고 생성형 AI 답변을 받는 구조는 같습니다. 다만 과제 문구가 OpenAI 사용을 엄격하게 지정했다면 Gemini 대체 가능 여부를 담당자에게 확인해야 합니다.

### 비용은 어떻게 제한했나요?

AI에는 최근 대화 12개만 전달하고 최대 출력은 700토큰으로 제한했습니다. 개발 시에는 가상 데이터를 한 번에 생성하고 불필요한 AI 요청을 줄였습니다.

### 한계와 개선점은 무엇인가요?

현재는 학교 과제용 단일 사용자 서비스라 로그인과 사용자별 데이터 분리가 없습니다. 건강 답변은 생활 습관 참고용이며 의료 진단이 아닙니다. 다음 단계에서는 Firebase Authentication, 사용자별 컬렉션, Function Calling이나 MCP 연동을 추가할 수 있습니다.

## 6. 제출 전 마지막 체크

- 프론트엔드, 백엔드, Swagger URL이 열리는지 확인
- 대시보드에 100건 이상의 데이터와 요약이 보이는지 확인
- 채팅 질문과 AI 답변, 로딩 표시 확인
- 데이터 추가 또는 수정·삭제 시연 확인
- 대화 목록과 특정 대화 불러오기 확인
- `SUBMISSION.md`의 네 장의 스크린샷 확인
- Gemini 대체 사용 허용 여부를 담당자에게 확인
- 발표 직전 Render 무료 서버 미리 깨우기
